{
  "name": "trestle",
  "version": "0.1.0",
  "description": "Knowledge graph harness for code migration: extract facts from any corpus, resolve a typed evidence graph, project boundaries, and serve it to coding agents.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=23.6"
  },
  "bin": {
    "trestle": "./bin/trestle.js"
  },
  "files": [
    "bin",
    "dist",
    "src",
    "skills",
    "examples",
    ".amp/plugins"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "test": "node --test 'tests/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@ladybugdb/core": ">=0.19.0"
  },
  "peerDependenciesMeta": {
    "@ladybugdb/core": {
      "optional": true
    }
  },
  "devDependencies": {
    "@ladybugdb/core": "^0.19.1",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
